y_count =1;
temp_count = 1; 
convolution_counter = 1;
result = numeric(max(x,y) + 1)
while ((y_count <= length(y)) || (temp_count <= length(temp)) )
{
	if(y_count > length(y))
		y_count = length(y)
	result[i]
	y_count = y_count + 1
	temp_count = temp_count + 1
} 
print(y_count)
print(temp_count)
